# Pitstop

### What is PitStop?
## Table of Content

## GitHub Command

- `git clone <repo_clone_link>`
- `git checkout -b <branch_name>`
- `git add .`
- `git commit -m "<msg>"`
- `git push origin <branch_name>`
